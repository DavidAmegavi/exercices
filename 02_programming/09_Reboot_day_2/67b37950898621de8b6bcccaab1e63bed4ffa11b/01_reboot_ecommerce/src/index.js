const readline = require("readline");
const eCommerce = require("./eCommerce");

const reader = readline.createInterface({
  input: process.stdin,
  output: process.stdout,
});

eCommerce(reader);


1 start the eCommerce program
2 the programe wait the user to choose the part he want 
3 the user choose an operation 
4 if the user choose buy a product
5 then the app 
